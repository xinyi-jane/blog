# GitHub OAuth App 配置（用于 OAuth 代理服务）
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
OAUTH_REDIRECT_URI=https://your-oauth-proxy.vercel.app/api/auth?provider=github

# 站点 URL（可选，用于构建时）
SITE_URL=https://yourusername.github.io

