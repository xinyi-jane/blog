# GitHub Pages 部署指南

## 前置准备

1. 确保你有一个 GitHub 账号
2. 决定仓库名称：
   - 如果仓库名是 `username.github.io`，网站将部署到 `https://username.github.io`
   - 如果仓库名是其他名称（如 `blog`），网站将部署到 `https://username.github.io/blog`

## 部署步骤

### 步骤 1：在 GitHub 创建仓库

1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. 填写仓库信息：
   - Repository name: `username.github.io` 或 `blog`（根据你的选择）
   - 选择 Public
   - **不要**勾选 "Initialize this repository with a README"
4. 点击 "Create repository"

### 步骤 2：初始化本地 Git 仓库

在项目目录下执行：

```bash
cd /Users/xinyi/blog

# 初始化 git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 个人博客网站"
```

### 步骤 3：配置站点 URL

编辑 `astro.config.mjs`，修改 `site` 字段：

**如果仓库名是 `username.github.io`：**
```js
site: 'https://yourusername.github.io',
base: '/',
```

**如果仓库名是其他名称（如 `blog`）：**
```js
site: 'https://yourusername.github.io',
base: '/blog/',  // 注意：末尾要有斜杠
```

将 `yourusername` 替换为你的 GitHub 用户名。

### 步骤 4：连接远程仓库并推送

```bash
# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/yourusername/your-repo-name.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步骤 5：启用 GitHub Pages

1. 在 GitHub 仓库页面，点击 "Settings"
2. 在左侧菜单找到 "Pages"
3. 在 "Source" 部分：
   - 选择 "GitHub Actions"（不是 "Deploy from a branch"）
4. 保存设置

### 步骤 6：等待自动部署

1. 推送代码后，GitHub Actions 会自动开始构建
2. 在仓库页面点击 "Actions" 标签查看构建进度
3. 构建完成后（通常 2-5 分钟），你的网站就可以访问了
4. 访问地址：
   - `https://yourusername.github.io`（如果仓库名是 `username.github.io`）
   - `https://yourusername.github.io/blog`（如果仓库名是 `blog`）

## 后续更新

每次更新内容后，只需：

```bash
git add .
git commit -m "更新内容"
git push
```

GitHub Actions 会自动重新构建并部署。

## 注意事项

### 关于 Decap CMS

如果你要使用在线发布功能（Decap CMS），需要：

1. **配置 GitHub OAuth**（见 `OAUTH_SETUP.md`）
2. **更新 `public/admin/config.yml`**：
   - 修改 `repo` 为你的仓库路径
   - 修改 `base_url` 为你的 OAuth 代理地址

### 关于图片

- 图片存放在 `public/images/` 目录
- 确保图片文件也提交到 Git 仓库
- 注意仓库大小限制（GitHub 建议单个文件不超过 100MB）

### 常见问题

**Q: 构建失败？**
- 检查 GitHub Actions 日志中的错误信息
- 确保 `package.json` 中的依赖都正确
- 检查 `astro.config.mjs` 中的配置是否正确

**Q: 网站显示 404？**
- 检查 `base` 配置是否正确
- 确保 GitHub Pages 设置中选择了 "GitHub Actions"
- 等待几分钟让 DNS 生效

**Q: 样式不显示？**
- 检查 `base` 配置，确保路径正确
- 清除浏览器缓存

## 验证部署

部署成功后，你可以：

1. 访问你的网站 URL
2. 检查所有页面是否正常显示
3. 测试响应式设计（在不同设备上查看）
4. 检查 `/admin` 页面（如果已配置 OAuth）

祝你部署顺利！🎉

