---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';

// 获取最新的文章和动态
const allPosts = await getCollection('posts', ({ data }) => !data.draft);
const allUpdates = await getCollection('updates', ({ data }) => !data.draft);

// 按日期排序
const sortedPosts = allPosts.sort((a, b) => 
  b.data.date.getTime() - a.data.date.getTime()
);
const sortedUpdates = allUpdates.sort((a, b) => 
  b.data.date.getTime() - a.data.date.getTime()
);

// 获取最新的几条
const latestPosts = sortedPosts.slice(0, 6);
const latestUpdates = sortedUpdates.slice(0, 5);

// 渲染动态内容
const renderedUpdates = await Promise.all(
  latestUpdates.map(async (update) => {
    const { Content } = await update.render();
    return { ...update, Content };
  })
);

// 按频道分组
const lifePosts = sortedPosts.filter(p => p.data.channel === 'life').slice(0, 3);
const workPosts = sortedPosts.filter(p => p.data.channel === 'work').slice(0, 3);
---

<Layout title="首页" description="个人博客 - 记录生活与工作的点滴">
  <div class="space-y-16">
    <!-- 欢迎区域 -->
    <section class="text-center py-8">
      <h1 class="text-4xl md:text-5xl font-bold mb-4 bg-gradient-to-r from-mint-500 via-sky-400 to-rose-400 bg-clip-text text-transparent">
        记录生活与工作的点滴
      </h1>
      <p class="text-gray-500 text-lg">分享美好，记录成长</p>
    </section>

    <!-- 频道入口 -->
    <section class="grid md:grid-cols-2 gap-6">
      <a href="/life" class="group block p-8 bg-white/70 backdrop-blur-sm rounded-2xl border border-mint-100 shadow-sm hover:shadow-md hover:border-mint-300 transition-all duration-300 hover:-translate-y-1">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-12 h-12 rounded-full bg-gradient-to-br from-mint-200 to-mint-300 flex items-center justify-center text-2xl">
            🌸
          </div>
          <h2 class="text-2xl font-bold text-gray-800 group-hover:text-mint-600 transition-colors">生活</h2>
        </div>
        <p class="text-gray-600 mb-4">记录生活中的美好瞬间</p>
        <p class="text-sm text-mint-500 font-medium group-hover:translate-x-2 transition-transform inline-block">查看全部 →</p>
      </a>
      <a href="/work" class="group block p-8 bg-white/70 backdrop-blur-sm rounded-2xl border border-sky-100 shadow-sm hover:shadow-md hover:border-sky-300 transition-all duration-300 hover:-translate-y-1">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-12 h-12 rounded-full bg-gradient-to-br from-sky-200 to-sky-300 flex items-center justify-center text-2xl">
            💼
          </div>
          <h2 class="text-2xl font-bold text-gray-800 group-hover:text-sky-600 transition-colors">工作</h2>
        </div>
        <p class="text-gray-600 mb-4">分享工作中的思考与经验</p>
        <p class="text-sm text-sky-500 font-medium group-hover:translate-x-2 transition-transform inline-block">查看全部 →</p>
      </a>
    </section>

    <!-- 最新动态 -->
    {latestUpdates.length > 0 && (
      <section>
        <div class="flex items-center justify-between mb-8">
          <div class="flex items-center gap-3">
            <div class="w-1 h-8 bg-gradient-to-b from-rose-300 to-rose-400 rounded-full"></div>
            <h2 class="text-3xl font-bold text-gray-800">最新动态</h2>
          </div>
          <a href="/updates" class="text-rose-400 hover:text-rose-500 font-medium transition-colors">查看全部 →</a>
        </div>
        <div class="space-y-5">
          {renderedUpdates.map((update) => (
            <article class="p-6 bg-white/70 backdrop-blur-sm border border-rose-100 rounded-xl shadow-sm hover:shadow-md transition-all">
              <div class="flex items-start gap-4">
                <div class="w-2 h-2 rounded-full bg-rose-400 mt-2 flex-shrink-0"></div>
                <div class="flex-1">
                  <time class="text-sm text-gray-400 font-medium">
                    {update.data.date.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' })}
                  </time>
                  {update.data.title && (
                    <h3 class="text-lg font-semibold mt-2 text-gray-800">{update.data.title}</h3>
                  )}
                  <div class="mt-3 prose prose-sm max-w-none text-gray-600">
                    <update.Content />
                  </div>
                  {update.data.images && update.data.images.length > 0 && (
                    <div class="mt-4 grid grid-cols-3 gap-3">
                      {update.data.images.slice(0, 3).map((img) => (
                        <img src={img} alt="" class="w-full h-28 object-cover rounded-lg shadow-sm" />
                      ))}
                    </div>
                  )}
                </div>
              </div>
            </article>
          ))}
        </div>
      </section>
    )}

    <!-- 最新文章 -->
    {latestPosts.length > 0 && (
      <section>
        <div class="flex items-center gap-3 mb-8">
          <div class="w-1 h-8 bg-gradient-to-b from-mint-300 to-sky-400 rounded-full"></div>
          <h2 class="text-3xl font-bold text-gray-800">最新文章</h2>
        </div>
        <div class="grid md:grid-cols-2 gap-6">
          {latestPosts.map((post) => (
            <a href={`/${post.data.channel}/${post.slug}`} class="group block bg-white/70 backdrop-blur-sm border border-gray-100 rounded-2xl overflow-hidden shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-1">
              {post.data.cover && (
                <div class="overflow-hidden">
                  <img src={post.data.cover} alt={post.data.title} class="w-full h-48 object-cover group-hover:scale-105 transition-transform duration-300" />
                </div>
              )}
              <div class="p-6">
                <div class="flex items-center gap-2 mb-3">
                  <span class={`px-3 py-1 rounded-full text-xs font-medium ${
                    post.data.channel === 'life' 
                      ? 'bg-mint-100 text-mint-600' 
                      : 'bg-sky-100 text-sky-600'
                  }`}>
                    {post.data.channel === 'life' ? '🌸 生活' : '💼 工作'}
                  </span>
                  <time class="text-xs text-gray-400">{post.data.date.toLocaleDateString('zh-CN')}</time>
                </div>
                <h3 class="text-xl font-bold mb-2 text-gray-800 group-hover:text-mint-600 transition-colors">{post.data.title}</h3>
                {post.data.description && (
                  <p class="text-gray-600 text-sm mb-4 line-clamp-2">{post.data.description}</p>
                )}
                <p class="text-sm text-gray-400 group-hover:text-mint-500 transition-colors">阅读更多 →</p>
              </div>
            </a>
          ))}
        </div>
      </section>
    )}
  </div>
</Layout>

